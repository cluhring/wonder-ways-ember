<div id="search">
    <button type="button" class="close">×</button>
    <form>
      {{input value=searchTerm placeholder="Search Trails" autocomplete="off"}}
    </form>
</div>

<ul>
  {{#each filteredTrails as |trail|}}
  <li>{{#link-to 'trail' trail}}{{trail.name}}{{/link-to}}</li>
  {{/each}}
</ul>
